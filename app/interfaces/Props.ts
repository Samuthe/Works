import Subscription from "./Subscription";


/**
 * Props interface  for the Subscriptions component
 * @interface Props
 * @exports Props
 * @see Subscription
 * @returns {void}
 */
export default interface Props {
  subscriptions: Subscription[];
}
